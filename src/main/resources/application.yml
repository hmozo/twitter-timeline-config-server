
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hmozo/twitter-timeline-config-server.git
          default-label: master
          clone-on-start: true