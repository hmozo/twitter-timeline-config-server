
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hmozo/git-repo-config-test.git
          default-label: main
          clone-on-start: true